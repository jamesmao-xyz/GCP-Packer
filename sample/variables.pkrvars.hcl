project_id         = "[project_id for the vm]" 
network_project_id = "[network_project_id]"
subnetwork         = "[subnet-name for the temp vm to be provisioned]"
zone               = "[gcp zone for the vm]"
project_sa         = "service account has compute.admin role"